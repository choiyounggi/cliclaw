{
  "name": "cliclaw",
  "version": "0.4.1",
  "type": "module",
  "private": true,
  "description": "Telegram bridge for local coding CLIs (Claude Code, Codex, Pi) with per-chat sessions, confirm gate, streaming, image attachments.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/choiyounggi/cliclaw.git"
  },
  "scripts": {
    "start": "bun run bot.ts",
    "dev": "bun run --hot bot.ts",
    "test": "bun --bun x vitest run",
    "test:watch": "bun --bun x vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^25.7.0",
    "vitest": "^2.1.0"
  }
}
